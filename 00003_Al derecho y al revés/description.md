Transformamos elementos agrupadores en bloque a agrupadores en línea… ¿Podremos hacerlo al revés? :scream: 

> Creá la regla CSS para que el agrupador en línea `em` tenga `block` como atributo de `display`.